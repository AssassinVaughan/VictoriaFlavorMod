#Make cities smaller test
NGraphics = {
	DYNAMIC_TERRAIN_ROAD_WIDTH = 1.5		# was 2.5

	CELL_SCORE_BASE = 0.5					# A cell that is 100% within the province will get the full base score. A cell outside the province will get 0. A 50/50 cell will get approximately half base score.
	CELL_SCORE_SLOPE_MIN = 0.035			# amount of slope allowed before penalty kicks in. value corresponds to the slope's 'k' in f(x)=kx+m
	CELL_SCORE_SLOPE_PENALTY = 35			# penalty per 'k' above the limit. Does not scale linearly with slope angle so might be tricky to tweak
}

NCities = {
	GRID_SCALE = 0.7					# was 1.1
	MESH_SCALE = 0.6					# was 1.0

	CITY_ROAD_DISTANCE_BIAS = -0.75
	FARM_ROAD_DISTANCE_BIAS = 0.25
	MINE_ROAD_DISTANCE_BIAS = 0.25
	PORT_ROAD_DISTANCE_BIAS = -1.00
	WOOD_ROAD_DISTANCE_BIAS = 0.25
}
